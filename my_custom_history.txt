ls
cd Documents
ls
cd /home/hafmed/Documents/24-25/Desktop_thakir_prayer_times/CompilationLinux/
./linuxdeployqt-continuous-x86_64.AppImage thakir_prayer_times -appimage -unsupported-allow-new-glibc -qmake=/home/hafmed/Qt/6.9.1/gcc_64/bin/qmake -no-translations
./linuxdeployqt-continuous-x86_64.AppImage thakir_prayer_times -appimage -unsupported-allow-new-glibc -qmake=/home/hafmed/Qt/6.9.1/gcc_64/bin/qmake -no-translations
./Thakir_Prayer_Time-x86_64.AppImage
git clone https://invent.kde.org/kde-linux/kde-linux
cd kde-linux
sudo ./update.sh update
git clone https://invent.kde.org/kde-linux/kde-linux
cd kde-linux
sudo ./update.sh update
sudo apt install flatpak flatpak-builder
sudo install flatpak flatpak-builder
sudo pacman -S --needed flatpak flatpak-builder
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

help
paru -S flatpak flatpak-builder
flatpak remote-add --user --if-not-exists flathub-apps-built-locally https://flathub.org/repo/flathub.flatpakrepo
# Create folder to hold KDE source code; skippable if it already exists
mkdir -p ~/kde/src/
# Go into that folder
cd ~/kde/src/
# Download source code for Filelight. Skippable if already done
git clone git@invent.kde.org:utilities/filelight.git
# Go into Filelight's source code folder
cd ~/kde/src/filelight
# Build Filelight as a Flatpak
flatpak-builder build --user --install-deps-from=flathub-apps-built-locally --force-clean --ccache --install .flatpak-manifest.json
# Create folder to hold KDE source code; skippable if it already exists
mkdir -p ~/kde/src/
# Go into that folder
cd ~/kde/src/
# Download source code for Filelight. Skippable if already done
git clone git@invent.kde.org:utilities/filelight.git
# Go into Filelight's source code folder
cd ~/kde/src/filelight
# Build Filelight as a Flatpak
flatpak-builder build --user --install-deps-from=flathub-apps-built-locally --force-clean --ccache --install .flatpak-manifest.json
sudo
root
sudo # Create folder to hold KDE source code; skippable if it already exists
mkdir -p ~/kde/src/
# Go into that folder
cd ~/kde/src/
# Download source code for Filelight. Skippable if already done
git clone git@invent.kde.org:utilities/filelight.git
# Go into Filelight's source code folder
cd ~/kde/src/filelight
# Build Filelight as a Flatpak
flatpak-builder build --user --install-deps-from=flathub-apps-built-locally --force-clean --ccache --install .flatpak-manifest.json
cd /home/hafmed/Documents/24-25/Desktop_thakir_prayer_times/CompilationLinux/
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml
flatpak install flathub org.kde.Sdk/x86_64/6.9
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml
cd /home/hafmed/Documents/24-25/Desktop_thakir_prayer_times/src/
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml --force-clean
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml --force-clean
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml --force-clean
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml --force-clean
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml --force-clean
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml --force-clean
    flatpak-builder --repo=repo build-dir thakir_prayer_times.yaml --force-clean
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir org.flatpak.thakir_prayer_times.yml
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir thakir_prayer_times.yml
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir thakir_prayer_times.yaml
flatpak remote-add --if-not-exists --user flathub https://dl.flathub.org/repo/flathub.flatpakrepo
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir thakir_prayer_times.yaml
cd /home/hafmed/Documents/24-25/basic_numerical_methods_qml/
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir appbasic_numerical_methods_qml.yaml
flatpak run org.flatpak.appbasic_numerical_methods_qml
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir appbasic_numerical_methods_qml.yaml
flatpak run org.flatpak.appbasic_numerical_methods_qml
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir appbasic_numerical_methods_qml.yaml
flatpak run org.flatpak.appbasic_numerical_methods_qml
    flatpak-builder --repo=repo build-dir appbasic_numerical_methods_qml.yaml --force-clean
flatpak run org.flatpak.appbasic_numerical_methods_qml
flatpak-builder --repo=repo build-dir appbasic_numerical_methods_qml.yaml --force-clean
flatpak run org.flatpak.appbasic_numerical_methods_qml
flatpak-builder --force-clean --user --install-deps-from=flathub --repo=repo --install builddir appbasic_numerical_methods_qml.yaml
flatpak run org.flatpak.appbasic_numerical_methods_qml
# PATH=/home/hafmed/Qt/6.9.1/gcc_64/bin:$PATH                ✔  2m 26s 
# export PATH
# PATH=/home/hafmed/Qt/6.9.1/gcc_64/bin:$PATH
# export PATH
flatpak run org.flatpak.appbasic_numerical_methods_qml
history -a
history -w ~/my_custom_history.txt
